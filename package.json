{
  "name": "compta-os",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"wait-on http://127.0.0.1:3001/api/health --timeout 30000 && npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "electron": "electron .",
    "electron:dev": "concurrently --names \"backend,frontend,electron\" --prefix-colors \"cyan,magenta,yellow\" \"npm run dev --prefix backend\" \"wait-on http://127.0.0.1:3001/api/health --timeout 30000 && npm run dev --prefix frontend\" \"wait-on http://127.0.0.1:3001/api/health --timeout 30000 && electron .\"",
    "electron:build": "npm run build && electron-builder",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "concurrently": "^8.2.2",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "wait-on": "^9.1.0"
  },
  "overrides": {
    "brace-expansion": "5.0.8"
  }
}
