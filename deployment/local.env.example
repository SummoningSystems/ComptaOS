# Set both values to the server's fixed LAN IP for access from other devices.
COMPTAOS_ADDRESS=localhost
COMPTAOS_BIND_IP=127.0.0.1
# Optional existing local OCR worker (no cloud fallback).
OCR_LOCAL_URL=
